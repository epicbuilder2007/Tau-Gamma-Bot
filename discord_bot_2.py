import os
import codecs
from discord.ext import commands
from dotenv import load_dotenv
import json

load_dotenv()
bot = commands.Bot(command_prefix="!")

class shipCommand:
    def info(self, ctx, ship, isSadistic):


@bot.command(name='info')
async def info(ctx, *, argv):

